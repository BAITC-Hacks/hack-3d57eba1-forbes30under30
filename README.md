# `<Название проекта>`

> HackAlem AI 2026 · Трек: `<...>` · Кейс: `<...>` · Команда: `<...>`

`<Одно предложение: что делает проект и для кого.>`

**Деплой:** `<ссылка или «нет»>` · **Видео демо:** `<ссылка или «нет»>`

## Проблема

`<Кто страдает, когда, сколько это стоит.>`

## Что реализовано

- `<функция 1>`
- `<функция 2>`
- `<функция 3>`

## Соответствие требованиям кейса

| # | Требование из ТЗ кейса | Реализовано | Где смотреть |
|---|---|---|---|
| 1 | `<...>` | ✅ | `<путь / экран>` |
| 2 | `<...>` | ✅ | `<...>` |
| 3 | `<...>` | ⚠️ частично | `<что именно>` |

## Как работает

`<Основной сценарий от входа до результата в 4–6 шагах. Что делает агент на каждом шаге.>`

## Архитектура

```
<схема: UI → API → Агент (tools) → Данные / внешние сервисы>
```

Компоненты:
- `<компонент>` — `<за что отвечает>`

## Технологии

- Node.js 20 LTS, TypeScript, Next.js (App Router), Tailwind CSS
- LLM: OpenAI `<модель>` через официальный SDK `openai`, function calling для инструментов агента
- Валидация: `zod`
- Open-source компоненты и модели: `<название — лицензия — как используется>`

## Требования

- Node.js 20 LTS (версия зафиксирована в `.nvmrc`) и npm
- Ключ OpenAI API (см. «Переменные окружения»)
- Docker не требуется

## Установка

```bash
git clone <url>
cd <папка>
npm install
cp .env.example .env
# заполнить OPENAI_API_KEY в .env
```

## Переменные окружения

| Переменная | Обязательна | Описание | Как получить |
|---|---|---|---|
| `OPENAI_API_KEY` | да | Ключ OpenAI API | platform.openai.com → API keys |
| `<...>` | нет | `<...>` | `<...>` |

## Запуск

```bash
npm run dev
```

После запуска открыть: http://localhost:3000

Прод-режим: `npm run build && npm start`

Быстрая проверка без интерфейса: `npm run smoke` — прогоняет основной сценарий
на демо-данных из `data/sample/` и завершается с кодом 0 при успехе.

## Как проверить (сценарий для экспертов)

1. Запустить проект: `npm run dev`.
2. `<Открыть ... / загрузить файл data/sample/... / ввести ...>`
3. `<Нажать ... / дождаться ...>`
4. Ожидаемый результат: `<что должно появиться на экране, время выполнения>`

Демо-данные: `data/sample/`

Проверка без личных аккаунтов: нужен только ключ OpenAI API в `.env`.

## Данные и интеграции

- Данные: `<источник, формат, объём>`
- Внешние сервисы: `<OpenAI API, ...>`

## Ограничения

- `<что не реализовано>`
- `<где решение может ошибаться>`
- `<ограничения текущей версии>`

## Команда

- `<Имя>` — `<роль, что делал>`
- `<Имя>` — `<роль, что делал>`
- `<Имя>` — `<роль, что делал>`
